name: RDP KDE Plasma (VPN/Axiolas version)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Create RDP User with Secure Password
        run: |
          set -euxo pipefail
          PASSWORD=$(openssl rand -base64 12)
          echo "RDP_USER=rdpuser" >> $GITHUB_ENV
          echo "RDP_PASS=$PASSWORD" >> $GITHUB_ENV

          sudo useradd -m -s /bin/bash rdpuser || true
          echo "rdpuser:$PASSWORD" | sudo chpasswd
          sudo usermod -aG sudo rdpuser

      - name: Install KDE Plasma + XRDP (fixed HOME)
        run: |
          set -euxo pipefail
          sudo apt-get update

          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xrdp xorgxrdp \
            kde-plasma-desktop plasma-workspace \
            dbus-x11 x11-xserver-utils

          sudo mkdir -p /home/rdpuser
          sudo chown -R rdpuser:rdpuser /home/rdpuser
          sudo chmod 755 /home/rdpuser

          sudo -u rdpuser bash -lc 'echo "startplasma-x11" > ~/.xsession'

          sudo bash -c 'cat > /etc/xrdp/startwm.sh' << "EOF"
          #!/bin/sh
          export USER="${LOGNAME:-$(id -un)}"
          export HOME="/home/$USER"
          export XDG_CONFIG_HOME="$HOME/.config"
          export XDG_CACHE_HOME="$HOME/.cache"
          export XDG_DATA_HOME="$HOME/.local/share"

          rm -f "$HOME/.Xauthority" "$HOME/.ICEauthority"
          mkdir -p "$HOME"
          chown -R "$USER:$USER" "$HOME" || true

          exec startplasma-x11
          EOF

          sudo chmod +x /etc/xrdp/startwm.sh
          sudo adduser xrdp ssl-cert || true
          sudo systemctl restart xrdp

      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          hostname: gh-rdp-${{ github.run_id }}

      - name: Show RDP Access (IP + creds)
        run: |
          set -euxo pipefail
          TS_IP=$(tailscale ip -4)
          echo "======================"
          echo "RDP Address (Tailscale): $TS_IP"
          echo "User: $RDP_USER"
          echo "Pass: $RDP_PASS"
          echo "Port: 3389"
          echo "======================"

          # quick check
          sudo apt-get install -y netcat-openbsd
          nc -zv localhost 3389

      - name: Keep Session Alive
        run: |
          while true; do
            echo "RDP Active..."
            sleep 300
          done
