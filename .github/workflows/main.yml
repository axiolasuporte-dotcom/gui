name: Ubuntu RDP (Modern KDE Plasma)

on:
  workflow_dispatch:

jobs:
  secure-rdp-linux:
    runs-on: ubuntu-22.04
    timeout-minutes: 3600

    steps:
      - name: Create RDP User with Secure Password
        run: |
          PASSWORD=$(openssl rand -base64 12)
          echo "RDP_CREDS=User: rdpuser | Password: $PASSWORD" >> $GITHUB_ENV
          
          sudo useradd -m -s /bin/bash rdpuser
          echo "rdpuser:$PASSWORD" | sudo chpasswd
          sudo usermod -aG sudo rdpuser

      - name: Install Modern Desktop Environment (KDE Plasma) and XRDP
        run: |
          sudo apt-get update
          # Install KDE Plasma (Modern UI) and XRDP
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y kde-plasma-desktop xrdp netcat-openbsd
          
          # Configure XRDP to use KDE Plasma properly
          sudo bash -c 'echo "startplasma-x11" > /home/rdpuser/.xsession'
          sudo chown rdpuser:rdpuser /home/rdpuser/.xsession
          
          sudo adduser xrdp ssl-cert
          sudo systemctl restart xrdp

      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          hostname: gh-runner-ubuntu-${{ github.run_id }}

      - name: Verify RDP Accessibility
        run: |
          sleep 5 
          
          TS_IP=$(tailscale ip -4)
          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV
          echo "Tailscale IP: $TS_IP"
          
          if nc -z localhost 3389; then
              echo "TCP connectivity successful! XRDP is listening."
          else
              echo "TCP connection to RDP port 3389 failed"
              exit 1
          fi

      - name: Maintain Connection
        run: |
          echo " "
          echo "=== RDP ACCESS ==="
          echo "Address: $TAILSCALE_IP"
          echo "$RDP_CREDS"
          echo "=================="
          echo " "
          
          while true; do
              echo "[$(date)] RDP Active - Cancel workflow manually to terminate"
              sleep 300
          done
